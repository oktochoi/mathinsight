# MathInsight 사용 가이드

학원(원장)·학부모·학생이 각각 다른 화면을 씁니다. 아래 **예시 시나리오**대로 따라 하면 처음부터 끝까지 연결할 수 있습니다.

- 기술·DB 설정: [SUPABASE_SETUP.md](./SUPABASE_SETUP.md)
- 개발 서버: `npm run dev` → [http://localhost:3000](http://localhost:3000)

---

## 역할별 로그인 주소

| 역할 | 회원가입 | 로그인 후 이동 |
|------|----------|----------------|
| 원장/강사 | `/signup` → **원장/강사** 선택 | `/dashboard` |
| 학부모 | `/signup` → **학부모** 선택 | `/parent` |
| 학생 | `/signup` → **학생** 선택 | `/student` |

로그인: `/login` — 가입할 때 고른 **역할과 동일하게** 선택해야 합니다.

---

## 예시 1: 새 학원 처음 세팅 (원장)

**상황:** 「햇살수학」을 처음 쓰는 원장 김원장

### 1) 원장 회원가입

1. `/signup` 접속
2. **원장/강사** 선택
3. 입력 예시  
   - 이름: `김원장`  
   - 학원 이름: `햇살수학`  
   - 이메일: `owner@hatssal.example.com`  
   - 비밀번호: 8자 이상
4. **회원가입** → `/login`으로 이동 안내가 나오면 **로그인**

### 2) 반 만들기

1. 로그인 후 왼쪽 메뉴 **Students**
2. 상단 **「반 이름 추가·수정」** 펼치기
3. 추가 예시  
   - `중2 A반` · 학년 `중2`  
   - `중2 B반` · 학년 `중2`  
   - `고1 심화반` · 학년 `고1`

### 3) 학생 등록

1. **학생 등록** 클릭
2. 예시 학생  

   | 이름 | 학년 | 반 | 학교 | 학부모 이메일 | 학생 이메일 |
   |------|------|-----|------|---------------|-------------|
   | 이준호 | 중2 | 중2 A반 | OO중 | *(아직 없으면 비움)* | *(아직 없으면 비움)* |
   | 박서연 | 중2 | 중2 B반 | OO중 | `mom.park@example.com` | `seoyeon@example.com` |

3. **저장** — 이메일은 학생 정보에 남습니다. 해당 이메일로 **이미 가입한** 학부모/학생만 자동 **연결 ✓** 됩니다.

### 4) 수업 기록 넣기 (부모·학생 화면에 보이려면 필수)

1. 메뉴 **Lesson Logs** (수업 기록)
2. 반 선택 → 날짜·단원·출석·숙제·점수 입력 → 저장

데이터가 없으면 학부모/학생 포털은 **연결됐어도 빈 화면**처럼 보일 수 있습니다.

### 5) 대시보드·상담·리포트

- **Dashboard**: 반·학생 상태 요약
- **Consultation Cards**: 학생 선택 후 기간 지정 → 상담 카드 생성
- **Parent Reports**: 학부모용 리포트 문구 생성

---

## 예시 2: 학부모·학생 계정 만들고 연결하기

**상황:** 이준호 학생의 엄마·본인이 앱에서 성적을 보게 하기

### 순서 (순서를 지키는 것이 중요합니다)

```
① 학부모·학생 각각 회원가입
② 원장이 학생 수정에서 가입 이메일 입력·저장
③ 목록에 부모 ✓ · 학생 ✓ 확인
④ 학부모/학생 로그인
```

### ① 학부모 회원가입

- `/signup` → **학부모**
- 이름: `이영희`  
- 이메일: `mom.junho@example.com` ← **원장이 나중에 넣을 이메일과 똑같이**

### ② 학생(자녀) 회원가입

- `/signup` → **학생**
- 이름: `이준호`  
- 이메일: `junho.student@example.com` ← **원장이 넣을 이메일과 동일**

### ③ 원장이 연결

1. 원장으로 `/students` → **이준호** → **수정**
2. 학부모 이메일: `mom.junho@example.com`
3. 학생 계정 이메일: `junho.student@example.com`
4. **저장**
5. 목록 **연결** 열: `부모 ✓ · 학생 ✓` 인지 확인  
   - `…` 만 보이면: 이메일 오타 또는 아직 해당 역할로 가입 안 함 → 가입 후 **다시 저장**

### ④ 학부모가 보는 화면

- `/login` → **학부모** → `mom.junho@example.com` 로그인
- `/parent`: 자녀 선택, 수업 요약·차트·**학부모 리포트**

### ⑤ 학생이 보는 화면

- `/login` → **학생** → `junho.student@example.com` 로그인
- `/student`: 오늘 수업·숙제·최근 점수·피드백

---

## 예시 3: 학생별로 한 명씩 보기 (원장)

1. **Students** → 필터 옆 **학생별 보기**
2. 왼쪽에서 `박서연` 클릭 → 오른쪽에 차트·최근 수업·포털 연결 상태
3. **상세** 버튼 또는 **정보 수정**으로 이메일·반 변경

---

## 예시 4: 하루 수업 입력 (원장)

**3월 15일 중2 A반 수업**

1. **Lesson Logs**
2. 반: `중2 A반`
3. 학생마다  
   - 출석: 출석 / 지각 / 결석  
   - 숙제: 완료 / 부분 / 미제출  
   - 점수: `85` (없으면 비움)
   - 단원: `일차방정식`  
   - 태그·메모: 선택
4. 일괄 저장

이후 해당 반 학생의 **상세**·**Dashboard**·**학부모/학생 포털**에 반영됩니다.

---

## 자주 묻는 문제

### 「연결된 자녀가 없습니다」(학부모)

- 원장이 **학생 수정**에 넣은 이메일 = 학부모 **회원가입 이메일**인지 확인
- Students 목록에 **부모 ✓** 인지 확인
- 원장이 **수업 기록**을 넣었는지 확인 (없어도 연결 메시지는 나와야 함)

### 학생 수정에서 이메일이 비어 있음

- Supabase에서 **`007_student_portal_emails.sql`** 실행 여부 확인
- 저장 후 **수정**을 다시 열어 이메일이 남는지 확인

### 「프로필이 없습니다」

- [SUPABASE_SETUP.md](./SUPABASE_SETUP.md) 의 **002, 005, 006** 실행 후 다시 로그인

### `infinite recursion … users`

- **`006_fix_users_rls_recursion.sql`** 실행

---

## 메뉴 한눈에 보기 (원장)

| 메뉴 | 하는 일 |
|------|---------|
| Dashboard | 학원 전체 요약·주의 학생 |
| Students | 반 추가, 학생 등록/수정, 학부모·학생 이메일 연결, 학생별 보기 |
| Lesson Logs | 수업·숙제·점수 입력 |
| Consultation Cards | 상담용 요약 카드 |
| Parent Reports | 학부모 전달용 리포트 |
| Settings | 학원 이름, 계정 이름, 반 관리 안내 |

---

## 체크리스트 (운영 시작 전)

- [ ] Supabase 마이그레이션 001 ~ 007 실행
- [ ] `.env.local` URL·키가 Dashboard와 동일 프로젝트
- [ ] 원장 가입 → 로그인 → Dashboard 진입
- [ ] 반 1개 이상 생성
- [ ] 학생 1명 이상 등록
- [ ] 수업 기록 1건 이상 입력
- [ ] (선택) 학부모·학생 가입 → 이메일 연결 → ✓ 확인 → 포털 로그인
